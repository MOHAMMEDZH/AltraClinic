# Round 18 Build Results

API build: `npx tsc -p tsconfig.build.json --pretty false` — **PASS_BASELINE** (exit 2, TS6059 permission-seeds/rootDir only).

`git diff --check`: PASS (see `11_DIFF_CHECK.txt`).
