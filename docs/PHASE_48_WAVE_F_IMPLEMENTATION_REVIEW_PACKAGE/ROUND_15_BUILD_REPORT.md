# Round 15 — Build Report

Command: `npx tsc -p tsconfig.build.json --pretty false`

Exit code: **2**

Classification: **PASS_BASELINE**

Diagnostics: unchanged known **TS6059** only (`prisma/seeds/permission-seeds.ts` vs `rootDir`). No new Wave F / Round 15 TypeScript diagnostics.

Raw: `ROUND_15_RAW_GATE_OUTPUTS/35_API_BUILD.txt`
