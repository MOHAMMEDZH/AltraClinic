# R4-F3 Secure Binding Provenance

Line fields: appointmentId, clinicalServiceId, snapshotRevisionId, courseSessionId, performanceBindingStatus
Bind requires durable provenance match; same patient+branch alone fails closed.