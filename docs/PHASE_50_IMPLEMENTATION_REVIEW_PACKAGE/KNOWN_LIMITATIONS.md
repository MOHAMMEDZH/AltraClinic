# Phase 50 — Known Limitations

- **Phase 50 PA** = **ACCEPTED** by CTO @ evidence tip `2d1f3ec` (not self-granted).
- **D5** AppointmentForm clinical-catalog picker = **DEFERRED (CTO)** — candidate documented in D1; not required for Phase 50 exit; not implemented.
- **Full `#scheduling-region` axe contrast** — legacy Wave H gap; D4 aligned progressive gate to H3 reception chrome + calendar controls instead of hub redesign.
- **Beauty / beauty-workspace / encounters-detail** `color-contrast` disableRules retained with DEFER comments — surface redesign, not a one-token fix (`docs/PHASE_50_D4_A11Y/`).
- **DashboardWidgets** patient primary labels — widget DTO has `patientId` only; D6 deferred name join (`docs/PHASE_50_D6_OWNER_NAMES/`).
- Owner-report / commission list DTOs still carry IDs; D6 joins via existing identity/inventory list APIs — fail-closed UUID-only when catalog/directory miss (pageSize / permission limits).
- Local Playwright a11y may skip when API / `E2E_API_READY` unavailable; **clinic-dashboard CI** remains authoritative e2e gate.
- Local **`.ci-evidence/phase50-*`** remains **uncommitted** by policy.
- Phase 51 commercial launch = **NOT AUTHORIZED**.
- Wave A–I / Phase 49 SoR = **closed** — not reopened by Phase 50 polish.
