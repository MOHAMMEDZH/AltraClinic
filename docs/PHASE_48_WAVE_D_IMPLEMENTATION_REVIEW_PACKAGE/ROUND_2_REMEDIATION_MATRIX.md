# Wave D Round 2 Remediation Matrix

| Blocker | Confirmed root cause | Production fix | DB invariant | Test proof | Files | Result |
|---------|---------------------|----------------|--------------|------------|-------|--------|
| B2 branch↔encounter | Same-tenant branchId could disagree with encounter.branchId when appointment absent | Reject mismatch; derive branchId from encounter only when omitted | N/A (app validation) | `wave-d-dental.postgres.integration.spec.ts` — mismatch reject, derive accept, no audit on fail | `service-performance.service.ts`, `wave-d-reference.validation.ts` | PASS |
| B2 snapshot context | Snapshot validated only for service/appointment filter, not patient/branch/encounter lineage | `assertTenantSnapshotRevisionContext` enforces derivable snapshot↔appointment↔patient/branch/encounter consistency | N/A | Same spec — patient/branch contextual mismatch reject; fully consistent accept | `wave-d-reference.validation.ts`, `service-performance.service.ts` | PASS |
| B4 child/parent tenant | Child.tenantId RLS pass + cross-tenant parent FK allowed dangerous inserts | BEFORE INSERT/UPDATE triggers on four child tables | Trigger tenant match on each parent FK | `wave-d-rls.postgres.integration.spec.ts` — mixed-tenant INSERT, parent-switch UPDATE, same-tenant INSERT all six tables | `migrations/20260819220000_phase48_wave_d_round2_child_tenant_integrity/migration.sql`, `triggers.sql` | PASS |
| B5 booking pricing path | Resolver-only proof; unit check after `lookupActivePrice`/`reconcileCommercialTimeline` | Move `assertPricingUnitAppliesToDomain` before price lookup/reconcile; `CreateAppointmentHandler` integration tests | N/A | `wave-d-pricing-production-path.postgres.integration.spec.ts` — handler success + PER_COURSE/PER_PACKAGE/mismatch reject with zero appointment/allocation/invoice mutation; SCHEDULED price unchanged | `booking-commercial-resolver.service.ts`, pricing spec | PASS |
| B6 dental consume path | No Wave D postgres proof of handler chain | `wave-d-dental-consume.postgres.integration.spec.ts` via `ConsumeDentalMaterialHandler` → `ConsumeInventoryHandler` → `InventoryUsagePostingService` | Existing `inventory_consumption_logs` only | 7 tests: valid refs, unknown/cross-tenant refs, accountability, ledger authority, rollback | `wave-d-dental-consume.postgres.integration.spec.ts` | PASS |
