/**
 * Step 06's scaffold layout has been replaced by the Step 09 design system
 * shell (`src/shell/AppShell.tsx`). This module is kept as a thin
 * re-export so any lingering imports of the old scaffold name keep
 * resolving to the current shell implementation.
 */
export { AppShell as ScaffoldLayout } from '../../shell/AppShell';
