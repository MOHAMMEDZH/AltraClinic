# Super Admin (Step 06+) — no secrets.

# Prefix: VITE_SUPER_ADMIN_* (plus VITE_API_BASE_URL for platform API)



VITE_SUPER_ADMIN_APP_NAME=Super Admin

VITE_SUPER_ADMIN_ENV=development

# Local API origin used by platform auth (must match API CORS allowlist)

VITE_API_BASE_URL=http://127.0.0.1:3000

